# Rename this distribution example file to motion.conf
#
# This config file was generated by motion 4.3.2
# Documentation:  /usr/share/doc/motion/motion_guide.html
#
# This file contains only the basic configuration options to get a
# system working.  There are many more options available.  Please
# consult the documentation for the complete list of all options.
#

############################################################
# System control configuration parameters
############################################################

# Start in daemon (background) mode and release terminal.
daemon off

# Start in Setup-Mode, daemon disabled.
setup_mode off

# File to store the process ID.
; pid_file value

# File to write logs messages into.  If not defined stderr and syslog is used.
log_file /var/log/motion.log

# Level of log messages [1..9] (EMG, ALR, CRT, ERR, WRN, NTC, INF, DBG, ALL).
log_level 6
# Target directory for pictures, snapshots and movies
target_dir /home/xxx/workspace/motion_record_local_media_files

# Video device (e.g. /dev/video0) to be used for capturing.
#video_device /dev/video0

# Parameters to control video device.  See motion_guide.html
; vid_control_params value

# The full URL of the network camera stream.
netcam_url rtsps://${your rtsp url}/live/stream

#netcam_params rtsp_uses_tcp=0


#audio
# Enable RTSP audio analysis and audio-triggered events.
# This controls only RTSP FFT audio detection. Motion pixel detection is unchanged.
netcam_audio_detection on

# Lower FFT band edge (Hz) used by RTSP audio detection.
# Keep this lower than netcam_audio_band_high.
netcam_audio_band_low 300

# Upper FFT band edge (Hz) used by RTSP audio detection.
# Default 3400 works well for speech-band energy; increase for wider-band sounds.
netcam_audio_band_high 3400

# RTSP audio trigger ratio (band_energy / total_energy).
# Accepts decimal (0.60) or percent-like value (60). Higher values reduce false triggers.
netcam_audio_trigger_ratio 0.60

# Debounce window: count trigger hits only within this many seconds.
netcam_audio_trigger_window_sec 5

# Required hit count inside the debounce window before emitting one audio event.
netcam_audio_trigger_hits 10

#audio

watchdog_tmo 30
watchdog_kill 0

# Name of mmal camera (e.g. vc.ril.camera for pi camera).
; mmalcam_name value

# Camera control parameters (see raspivid/raspistill tool documentation)
; mmalcam_control_params value

############################################################
# Image Processing configuration parameters
############################################################

# Image width in pixels.
width 800

# Image height in pixels.
height 480

# Maximum number of frames to be captured per second.
framerate 30
#framerate 8
#netcam_params rtsp_transport=tcp,capture_rate=30


# Text to be overlayed in the lower left corner of images
#text_left CAMERA1

# Text to be overlayed in the lower right corner of images.
#text_right %Y-%m-%d\n%T-%q

############################################################
# Motion detection configuration parameters
############################################################

# Always save pictures and movies even if there was no motion.
emulate_motion off

# Threshold for number of changed pixels that triggers motion.
threshold 1000

# Noise threshold for the motion detection.
; noise_level 32

# Despeckle the image using (E/e)rode or (D/d)ilate or (l)abel.
despeckle_filter EedDl

# Number of images that must contain motion to trigger an event.
minimum_motion_frames 1

# Gap in seconds of no motion detected that triggers the end of an event.
event_gap 20

# The number of pre-captured (buffered) pictures from before motion.
pre_capture 3

# Number of frames to capture after motion is no longer detected.
post_capture 0

############################################################
# Script execution configuration parameters
############################################################

# Command to be executed when an event starts.

# Command to be executed when an event ends.

# Command to be executed when a movie file is closed.

############################################################
# Picture output configuration parameters
############################################################

# Output pictures when motion is detected
picture_output off

# File name(without extension) for pictures relative to target directory
picture_filename %Y%m%d%H%M%S-%q

############################################################
# Movie output configuration parameters
############################################################

# Create movies of motion events.
movie_output on

# Maximum length of movie in seconds.
movie_max_time 60

# The encoding quality of the movie. (0=use bitrate. 1=worst quality, 100=best)
movie_quality 48

# Container/Codec to used for the movie. See motion_guide.html
movie_codec mp4

# File name(without extension) for movies relative to target directory
movie_filename %m_%d_%Y_%H_%M_%S

movie_passthrough on

#movie_max_time 300


############################################################
# Webcontrol configuration parameters
############################################################
webcontrol_ipv6 off
# Port number used for the webcontrol.
webcontrol_port 8888

# Restrict webcontrol connections to the localhost.
webcontrol_localhost off

# Type of configuration options to allow via the webcontrol.
webcontrol_parms 0

############################################################
# Live stream configuration parameters
############################################################

# The port number for the live stream.
stream_port 8889

# Restrict stream connections to the localhost.


##############################################################
# Camera config files - One for each camera.
##############################################################
; camera /usr/etc/motion/camera1.conf
; camera /usr/etc/motion/camera2.conf
; camera /usr/etc/motion/camera3.conf
; camera /usr/etc/motion/camera4.conf

##############################################################
# Directory to read '.conf' files for cameras.
##############################################################
; camera_dir /usr/etc/motion/conf.d

picture_output best



